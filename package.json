{
  "name": "screenboard",
  "version": "0.1.0",
  "private": true,
  "description": "Local screen mirror and live whiteboard app",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "dependencies": {
    "pg": "^8.12.0"
  },
  "engines": {
    "node": ">=18"
  }
}
