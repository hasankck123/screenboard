{
  "name": "screenboard",
  "version": "0.1.0",
  "private": true,
  "description": "Screen mirror and live whiteboard app",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
